<div style="padding-top:10px;" class="row centered">
			<% for product in @products %>
			  <div class="col-sm-4">
			    <h4>
			      <%= product.name %>
			    </h4>
			    <p style="font-size:13px; padding:0px 40px 0px 40px; margin-bottom:-1px;"><%= product.description %></p><br/>
			    <!-- <p style=" font-weight:bold; margin-bottom:-5px;">Donation Amount: <%#= number_to_currency(product.price) %></p><br/> -->
			<% end %>
			<%= render 'paypal1' %>
		 	</div>
		</div>